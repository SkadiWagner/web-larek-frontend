import { Form } from '../common/form';
import { Component } from '../base/component';
import { IEvents } from '../base/events';

export class SuccessComponent extends Component<any>{
	constructor(container: HTMLElement, events: IEvents) {
		super(container);
	}
}